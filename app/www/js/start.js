(function($, angular) {

	$(function() {

		angular.bootstrap($("body"), ["WidgetApp"]);

		// var element = $("[ng-app]");
		// angular.bootstrap(element[0], [element.attr("ng-app")]);

	});

})(jQuery, angular);
